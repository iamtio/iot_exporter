apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot_exporter.fullname" . }}
  labels:
    {{- include "iot_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "iot_exporter.selectorLabels" . | nindent 4 }}
